{
  "name": "agent-monitor",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --filter client build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
